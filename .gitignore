# ── large datasets — not committed (use organizer datasets locally) ──
# (the small committed SCADA sample lives in data/raw_data/ and is kept)
/raw_data/
*.large.csv
main_monitoring_data.csv
main_2017.csv
validation_2018.csv
raw_validation_2018.csv
synthetic_eval.csv
fault_test_dataset*.csv

# ── python ──
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ── generated artifacts: commit the JSON/txt the dashboard serves, ignore runtime log ──
out/audit.jsonl

# ── secrets ──
.env
*.env

# ── os / editor ──
.DS_Store
.idea/
.vscode/
